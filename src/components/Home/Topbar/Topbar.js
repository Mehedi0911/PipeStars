import React from 'react';

const Topbar = () => {
    return (
        <section className="brand-background-secondary">
            <h6>This is topbar</h6>
        </section>
    );
};

export default Topbar;